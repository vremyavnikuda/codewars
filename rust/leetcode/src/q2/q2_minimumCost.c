int minimumCost(int *cost, int costSize)
{
    int c[101] = {0}, s = 0, i = 0, j = 100;
    for (; i < costSize; ++c[cost[i++]])
        ;
    for (i = 0; j; c[j] ? i++ % 3 - 2 && (s += j), --c[j] : (--j))
        ;
    return s;
}
